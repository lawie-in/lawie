{
  "name": "@lawie/email-client",
  "version": "1.0.0",
  "description": "Lawie email producer — one-line enqueueEmail() API. Pushes jobs onto BullMQ; the email-worker drains them.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage --forceExit"
  },
  "dependencies": {
    "bullmq": "^5.12.0",
    "ioredis": "^5.10.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.2",
    "ioredis-mock": "^8.13.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.5"
  }
}
