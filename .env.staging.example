# ── App ───────────────────────────────────────────────────────────────────────
NODE_ENV=staging

# ── Service Ports ─────────────────────────────────────────────────────────────
GATEWAY_PORT=4000
AUTH_PORT=4001
DRAFTING_PORT=4002
BILLING_PORT=4003

# ── Gateway — service discovery (internal ECS DNS or Docker network names) ────
AUTH_SERVICE_URL=http://auth:4001
DRAFTING_SERVICE_URL=http://drafting:4002
BILLING_SERVICE_URL=http://billing:4003

# ── MongoDB Atlas ─────────────────────────────────────────────────────────────
# Injected from AWS Secrets Manager: /lawie/staging/MONGO_URI
MONGO_URI=mongodb+srv://lawie_user:CHANGE_ME@lawie-staging.xxx.mongodb.net/lawie?retryWrites=true&w=majority

# ── JWT ───────────────────────────────────────────────────────────────────────
# Injected from AWS Secrets Manager: /lawie/staging/JWT_SECRET
# Generate: openssl rand -hex 64
JWT_SECRET=CHANGE_ME
# Injected from AWS Secrets Manager: /lawie/staging/JWT_REFRESH_SECRET
JWT_REFRESH_SECRET=CHANGE_ME
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ── Encryption (drafting service — AES-256-GCM) ───────────────────────────────
# Injected from AWS Secrets Manager: /lawie/staging/ENCRYPTION_KEY
# Generate: openssl rand -hex 32
ENCRYPTION_KEY=CHANGE_ME

# ── Frontend ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=https://api.staging.lawie.com

# ── CORS ──────────────────────────────────────────────────────────────────────
ALLOWED_ORIGINS=https://staging.lawie.com

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=info

# ── AWS ───────────────────────────────────────────────────────────────────────
AWS_REGION=ap-south-1
ECR_REGISTRY=CHANGE_ME.dkr.ecr.ap-south-1.amazonaws.com

# ── Razorpay (billing service) ────────────────────────────────────────────────
# Injected from AWS Secrets Manager: /lawie/staging/RAZORPAY_*
RAZORPAY_KEY_ID=rzp_test_CHANGE_ME
RAZORPAY_KEY_SECRET=CHANGE_ME
RAZORPAY_WEBHOOK_SECRET=CHANGE_ME

# ── Email ─────────────────────────────────────────────────────────────────────
SMTP_HOST=email-smtp.ap-south-1.amazonaws.com
SMTP_PORT=587
SMTP_USER=CHANGE_ME
# Injected from AWS Secrets Manager: /lawie/staging/SMTP_PASS
SMTP_PASS=CHANGE_ME
EMAIL_FROM=noreply@staging.lawie.com

# ── Sentry ────────────────────────────────────────────────────────────────────
# Injected from AWS Secrets Manager: /lawie/staging/SENTRY_DSN
SENTRY_DSN=CHANGE_ME
NEXT_PUBLIC_SENTRY_DSN=CHANGE_ME
